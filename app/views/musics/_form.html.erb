<%= form_for @music, :html => { :class => 'form-horizontal' } do |f| %>
    <fieldset>
      <legend><%= controller.action_name.capitalize %> Music</legend>
      <div class="control-group">
        <%= f.label :name, :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :name, :class => 'text_field' %>
        </div>
      </div>
      <div class="control-group">
        <%= f.label :singer, :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :singer, :class => 'text_field' %>
        </div>
      </div>
      <div class="control-group">
        <%= f.label :janra, :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :janra, :class => 'text_field' %>
        </div>
      </div>
      <div class="form-actions">
        <%= f.submit nil, :class => 'btn btn-primary' %>
        <%= link_to 'Cancel', musics_path, :class => 'btn' %>
      </div>
    </fieldset>
<% end %>